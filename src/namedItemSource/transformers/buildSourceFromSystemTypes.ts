import type { Data_System } from "@RpgTypes/schema";
import {
  getArmorTypes,
  getElementTypes,
  getEquipTypes,
  getParamNames,
  getSkillTypes,
  getVariableNames,
  getWeaponTypes,
} from "./data/system";

const xx = () => {};

const buildElementTypesSource = (system: Pick<Data_System, "elements">) => {};
