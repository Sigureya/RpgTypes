import { LabelRegistry } from '../../schema';
import { DeepPartial } from '../../utils';
export declare const finalizeLabelRegistry: (base: LabelRegistry, label: DeepPartial<LabelRegistry>) => LabelRegistry;
