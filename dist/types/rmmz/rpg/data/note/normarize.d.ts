import { NormalizedNote } from './types';
export declare const buildNoteFromNormalized: (note: NormalizedNote, sep?: string) => string;
export declare const normalizeNote: (note: string) => NormalizedNote;
